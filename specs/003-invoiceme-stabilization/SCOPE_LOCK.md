# Scope Lock — 003 invoiceme stabilization

This spec session MUST implement ALL items 1–20 exactly as listed, with no omissions.

In addition:
- UI must be responsive for BOTH mobile/tablet AND web/desktop.
- Fix any regression where mobile improvements harmed web layout.
- No flow changes. No new screens. No new navigation.
- Any design changes are only to satisfy 1–20 and the web regression fix.

Done Definition:
- Each item 1–20 has: implementation, acceptance criteria, manual test, and verified pass.
- Flutter Web + Mobile: no overflow, no broken charts, no broken dialogs.
- No runtime type errors.
